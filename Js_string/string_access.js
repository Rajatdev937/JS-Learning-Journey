//STRING Length - we can find the length of a string using str.length function.But the wide spaces are also counted.

let str="bingo";
let a=str.length;
console.log(a);

//ACESSING string -  we can access the  character of string by indexing.

let str2="Computer";
console.log(str2[0]);
//or
console.log(str2.charAt(4));

